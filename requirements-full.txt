# Full requirements for local CLI usage (pipeline, scheduler, screenshots)
# Install: pip install -r requirements-full.txt

-r requirements.txt

lxml>=5.0.0
playwright>=1.40.0
aiohttp>=3.9.0
apscheduler>=3.10.0
crawl4ai>=0.8.0
